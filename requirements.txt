# Streamlit hosting deps. CPU-only torch/torchvision keep the HF Spaces build
# small and fast — the default wheels pull ~2GB of unused CUDA libraries.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.58.0     # keep in sync with sdk_version in README.md (HF Spaces)
pinecone>=6.0.0
boto3>=1.34.0            # S3 upload tool (scripts/upload_images_to_s3.py)
open-clip-torch>=2.20.0
torch==2.12.0+cpu
torchvision==0.27.0+cpu
pillow>=10.0.0
requests>=2.31.0
python-dotenv>=1.0.0
pandas>=2.0.0            # imported at runtime via embed_sparse_text
pyarrow>=14.0.0
tqdm>=4.66.0
pyyaml>=6.0
huggingface_hub>=0.23.0  # open_clip fetches the CLIP weights through it

# Removed for the Streamlit deploy (not imported by the running app):
#   ipython, matplotlib, jupyter  — notebooks / analysis only
#   transformers, soundfile, soxr — deferred FSD50K sound feature (re-add to re-enable)
